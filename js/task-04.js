const counterValue = 0;

const addListenerDecrementBtn = document.querySelector(
  '[data-action="decrement"]'
);

const addListenerIncrementBtn = document.querySelector(
  '[data-action="increment"]'
);
